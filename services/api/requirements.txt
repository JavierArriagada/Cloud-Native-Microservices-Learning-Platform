# =============================================================================
# FastAPI Backend - Production Dependencies
# =============================================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
asyncpg==0.29.0
alembic==1.13.1
sqlalchemy==2.0.25  # Solo para Alembic autogenerate, NO usar como ORM en runtime
psycopg2-binary==2.9.9  # Driver PostgreSQL para SQLAlchemy (solo para Alembic)

# Validation & Serialization
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Code Generation Tools
sqlacodegen==3.0.0  # Para generar SQLAlchemy models de DB existente
pydantic-sqlalchemy==0.0.9  # Para convertir SQLAlchemy -> Pydantic

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# HTTP Client (for calling other services)
httpx==0.26.0

# Monitoring (optional, add later)
# prometheus-fastapi-instrumentator==6.1.0

# Utilities
python-dotenv==1.0.0
